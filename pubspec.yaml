name: english_quiz_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  floor: ^1.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^7.2.0
  flutter_hooks: ^0.18.0
  flutter_localizations:
    sdk: flutter
  flutter_screenutil: ^5.0.0+2
  fluttertoast: ^8.0.8
  get_it: ^7.2.0
  intl: ^0.17.0
  retrofit: ^2.0.1
  supercharged: ^2.0.0
  tokenizer:
    git:
      url: git@github.com:Tokenizer94/tokenizer_flutter_package.git

dev_dependencies:
  build_runner: ^2.1.2
  floor_generator: ^1.1.0
  flutter_test:
    sdk: flutter
  lint: ^1.6.0
  retrofit_generator: any

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
